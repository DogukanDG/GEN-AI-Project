<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="shortcut icon" href="/assets/user.svg" type="image/x-icon">
    <title>Log in</title>
    <style>
      body {
        min-height: 100vh;
        display: flex;
      }
      #sidebar {
        min-width: 220px;
        max-width: 220px;
        min-height: 100vh;
        background: #7e70ff;
        color: #000000;
      }
      #sidebar .nav-link {
        color: #000000;
      }
      #sidebar .nav-link.active {
        background: #7aa3da;
      }
      main {
        flex: 1;
        padding: 2rem;
      }

      .fs-4 {
        color: #000000;
      }
    </style>
</head>

<body>
<% if (typeof alert !== 'undefined' && alert) { %>
  <div class="alert alert-<%= alert.type %> alert-dismissible fade show position-fixed end-0 bottom-0 m-4" role="alert" style="z-index: 9999; min-width: 300px;">
    <%= alert.message %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% } %>
<% if (typeof user !== 'undefined' && user) { %>
    <nav id="sidebar" class="d-flex flex-column p-3">
      <a href="/user/HomePage" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-white text-decoration-none">
        <img src="/assets/Navbar-Logo.png" alt="Logo" width="40" height="40" class="me-2 rounded">
        <span class="fs-4">AI Facility</span>
      </a>
      <hr>
      <ul class="nav nav-pills flex-column mb-auto">
        <li class="nav-item">
          <a href="/user/HomePage" class="nav-link">Home</a>
        </li>
        <li>
          <a href="/user/bookings" class="nav-link">My Bookings</a>
        </li>
        <li>
          <a href="/user/profile" class="nav-link">Profile</a>
        </li>
        <li>
          <a href="/user/logout" class="nav-link">Logout</a>
        </li>
      </ul>
    </nav>
<% } %>
    <main>